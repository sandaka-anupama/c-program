#include<stdio.h>
int main()
{
	int dig,sum,pro,n;
	printf("enter an integer");
	scanf("%d",&n);
	sum=0;
	pro=1;
	while(n>0)
	{
		dig=n%10;
		sum=sum+dig;
		pro=pro*dig;
		n=n/10;
	}
	printf("%d",pro-sum);
	
	return 0;
}
